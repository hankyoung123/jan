# Phase 0/1 Evidence Audit

This audit maps each Phase 0 and Phase 1 requirement in
`docs/product-plan.md` to current repository evidence. A requirement remains
open when evidence is missing, indirect, or contradicted by retained upstream
artifacts.

## Phase 0: repository and architecture freeze

| Requirement | Status | Current evidence | Remaining work |
|---|---|---|---|
| Fork Jan | Partial | `v0.8.4` (`5f30aee467f08941964a83f946e2663e7ae0e01f`) is an ancestor of the integration branch; ADR-0003 records the integration | Configure a user-owned GitHub Fork as `origin`; only `upstream` exists locally |
| New product repository | Partial | Root package, README, Python Sidecar, contracts, and product history identify Story Engine | A product remote is not configured |
| Add Jan upstream | Complete | Git remote `upstream` is `https://github.com/janhq/jan.git`; the exact tag is present | None |
| Lock Jan and Concordia | Complete for freeze | README, ADR-0001, notices, and migration map record Jan `v0.8.4`/`5f30aee…` and Concordia `v2.4.0`/`702998f…` | Install Concordia only when the adapter task begins |
| License inventory | Partial | `THIRD_PARTY_NOTICES.md` and `licenses/README.md` record included/excluded areas and package-level conflicts | Add exact per-component license texts; resolve retained AGPL package distribution before closed release |
| ADRs | Complete | ADR-0002 through ADR-0004 define canonical Markdown, Jan fork integration, and the single Jan model-runtime boundary | Add ADRs only for future core-technology changes |
| CI | Complete for development quality gates | `.github/workflows/story-engine-ci.yml` is the authoritative PR/push workflow: Web lint/tests/type-checked build, Python Ruff/mypy/pytest/package build, generated-contract drift/typecheck, whole-desktop Clippy, Story-owned Rust formatting, and a Tauri smoke build all pass locally; `productCi.test.ts` guards the quality contract; `productDistributionWorkflows.test.ts` proves Jan release, nightly, external-PR, AutoQA, Flatpak, portable, and npm-publish entrypoints are retired while inert `workflow_call` packaging templates remain | Adapt the retained packaging templates to Story Engine before adding any new release entrypoint; Jan v0.8.4 has no whole-tree rustfmt baseline, so formatting is intentionally enforced on Story-owned Rust boundary modules rather than mechanically rewriting retained upstream code |
| Windows and macOS launch | Partial | macOS application runtime has been exercised locally; inherited platform build workflows remain | Produce current Story Engine Windows launch evidence and repeat packaged macOS smoke verification |
| Python `/health` | Complete | `apps/story-engine/tests/api/test_health.py`; managed Tauri startup gates readiness on health polling | Preserve in CI |
| React shows Python status | Complete | `EngineStatus` route integration and tests; runtime UI displays the connected state | Preserve stopped/crashed/restart coverage |
| No old project database | Partial | Canonical Story Engine state is Markdown; no legacy Story project migration was imported | Add an explicit repository/fixture gate; inherited Jan mobile thread SQLite is platform infrastructure, not Story canonical state |

## Phase 1: product shell and page skeleton

| Requirement | Status | Current evidence | Remaining work |
|---|---|---|---|
| Replace product branding | Partial | Desktop titles, Web shell, localized product copy, logos, OpenAPI metadata, and marketing/docs distribution use Story Engine or are removed; active Jan product distribution workflows are retired; `productBranding.test.ts` and `productDistributionWorkflows.test.ts` are the gates | Audit and adapt Flatpak IDs, `jan-cli`, retained inert packaging templates, and other distributable metadata without renaming technical `@janhq` packages |
| Rewrite main navigation | Complete | Story navigation tests and runtime checks cover Workbench, Evolve, Characters, World, Events, Manuscript, Model Center, and settings | None |
| Keep model center and global settings | Complete | Jan model/Provider infrastructure remains authoritative and both routes render in the integrated shell | Preserve the ADR-0004 boundary |
| Establish product pages | Complete as skeleton | All required routes render and navigate without a 404 | Domain completion belongs to later phases |
| Remove or hide generic chat entry | Complete | Retired route, navigation, shortcut, and product-boundary tests cover inherited chat entry points | Continue deleting dead chat code only when it no longer supports retained infrastructure |
| Retain reusable Jan UI | Complete | The authoritative UI remains `web-app`; the parallel desktop shell was removed | Do not introduce a second shell |
| All main pages navigate | Complete | Route tests plus runtime traversal of all nine primary destinations | Preserve in Web tests |
| Light/dark themes | Complete | Inherited theme control and visual runtime checks pass in both modes | Preserve in UI smoke checks |
| Resize without layout breakage | Partial | Desktop-width runtime checks passed at multiple visible sizes | Add a repeatable narrow/minimum-window visual test |
| No old Jan brand assets | Partial | Jan logos, marketing site, screenshots, root banner/demo, and outdated translated promotional READMEs are removed | Complete the remaining distributable packaging/CLI audit above |

## Next local priorities

1. Audit and adapt `jan-cli` and Flatpak metadata without duplicating Jan
   runtime behavior; adapt the inert packaging templates only when Phase 10
   creates the Story Engine release entrypoint.
2. Add repeatable minimum-window and packaged desktop smoke evidence.
3. Complete the license-text inventory before any closed distribution.

Creating the user-owned GitHub Fork is intentionally not performed by a local
code change. It requires the target GitHub account/repository and creates
external state; once provided, configure that Fork as `origin` while retaining
Jan as `upstream`.

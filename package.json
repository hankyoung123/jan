{
  "name": "ai-story-evolution-engine-next",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": {
    "packages": [
      "core",
      "web-app",
      "extensions/*",
      "packages/contracts"
    ]
  },
  "scripts": {
    "lint": "yarn lint:jan && yarn lint:story",
    "lint:jan": "yarn workspace @janhq/web-app lint",
    "lint:story": "uv run --project apps/story-engine --extra dev ruff check apps/story-engine/src apps/story-engine/tests",
    "typecheck": "yarn typecheck:story",
    "typecheck:story": "uv run --project apps/story-engine --extra dev mypy --config-file apps/story-engine/pyproject.toml apps/story-engine/src/story_engine && yarn workspace @story-engine/contracts typecheck",
    "test": "yarn test:jan && yarn test:story",
    "test:jan": "yarn test:core && yarn test:web && yarn test:ext",
    "test:story": "uv run --project apps/story-engine --extra dev pytest -c apps/story-engine/pyproject.toml apps/story-engine/tests",
    "build": "yarn build:story && yarn bootstrap:jan && yarn build:web && yarn build:tauri",
    "build:story": "uv run --project apps/story-engine --extra dev python -m build apps/story-engine && yarn workspace @story-engine/contracts typecheck",
    "contracts:generate": "node scripts/generate-contracts.mjs",
    "contracts:check": "yarn contracts:generate && git diff --exit-code -- packages/contracts",
    "story-engine:dev": "uv run --project apps/story-engine story-engine serve --port 39281 --session-token development-token",
    "bootstrap:jan": "yarn build:extensions && yarn build:tauri:plugin:api",
    "dev": "yarn dev:tauri",
    "dev:web": "yarn workspace @janhq/web-app dev",
    "dev:tauri": "yarn build:icon && yarn copy:assets:tauri && cross-env IS_CLEAN=true tauri dev",
    "ios": "yarn tauri ios dev",
    "android": "yarn tauri android dev",
    "dev:ios": "yarn copy:assets:mobile && RUSTC_WRAPPER= cross-env IS_IOS=true yarn tauri ios dev --features mobile",
    "dev:android": "yarn copy:assets:mobile && cross-env IS_ANDROID=true yarn tauri android dev --features mobile",
    "test:core": "cross-env NODE_OPTIONS=--max-old-space-size=4096 vitest run --project @janhq/core",
    "test:web": "cross-env NODE_OPTIONS=--max-old-space-size=4096 vitest run --project @janhq/web-app",
    "test:visual": "playwright test --config playwright.config.ts",
    "test:ext": "cross-env NODE_OPTIONS=--max-old-space-size=4096 vitest run --project '@janhq/*-extension'",
    "test:all": "cross-env NODE_OPTIONS=--max-old-space-size=4096 vitest run",
    "test:watch": "cross-env NODE_OPTIONS=--max-old-space-size=4096 vitest",
    "test:ui": "cross-env NODE_OPTIONS=--max-old-space-size=4096 vitest --ui",
    "test:coverage": "cross-env NODE_OPTIONS=--max-old-space-size=4096 vitest run --coverage",
    "test:prepare": "yarn build:icon && yarn copy:assets:tauri && yarn build --no-bundle",
    "build:android": "yarn build:icon && yarn copy:assets:mobile && cross-env IS_CLEAN=true yarn tauri android build -- --no-default-features --features mobile",
    "build:ios": "yarn build:icon && yarn copy:assets:mobile && cross-env IS_IOS=true yarn tauri ios build -- --no-default-features --features mobile",
    "build:ios:device": "yarn build:icon && yarn copy:assets:mobile && cross-env IS_IOS=true yarn tauri ios build -- --no-default-features --features mobile --export-method debugging",
    "copy:assets:tauri": "cpx \"LICENSE\" \"src-tauri/resources/\"",
    "copy:assets:mobile": "cpx \"LICENSE\" \"src-tauri/resources/\"",
    "download:lib": "node ./scripts/download-lib.mjs",
    "download:bin": "node ./scripts/download-bin.mjs",
    "build:mlx-server": "make build-mlx-server",
    "build:cli": "make build-cli",
    "build:tauri:win32": "yarn download:bin && yarn build:cli && yarn tauri build",
    "build:tauri:linux": "yarn download:bin && yarn build:cli && NO_STRIP=1 ./src-tauri/build-utils/shim-linuxdeploy.sh yarn tauri build && ./src-tauri/build-utils/buildAppImage.sh",
    "build:tauri:darwin": "yarn download:bin && yarn build:mlx-server && yarn build:cli && yarn tauri build --target universal-apple-darwin",
    "build:tauri": "yarn build:icon && yarn copy:assets:tauri && run-script-os",
    "build:tauri:plugin:api": "cd src-tauri/plugins && yarn install && cd ../.. && run-script-os",
    "build:tauri:plugin:api:darwin": "cd src-tauri/plugins && yarn workspaces foreach -Apt run build",
    "build:tauri:plugin:api:win32": "cd src-tauri/plugins && yarn workspaces foreach -Apt --exclude @janhq/tauri-plugin-mlx-api run build",
    "build:tauri:plugin:api:linux": "cd src-tauri/plugins && yarn workspaces foreach -Apt --exclude @janhq/tauri-plugin-mlx-api run build",
    "build:icon": "tauri icon ./src-tauri/icons/icon.png",
    "build:core": "cd core && yarn build && yarn pack",
    "build:web": "yarn workspace @janhq/web-app build",
    "build:extensions": "yarn workspace @janhq/core build && yarn workspaces foreach -Apt --include '@janhq/*-extension' run build",
    "prepare": "husky"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@tauri-apps/cli": "^2.7.0",
    "@vitest/coverage-v8": "^3.1.3",
    "cpx": "^1.5.0",
    "cross-env": "^7.0.3",
    "husky": "^9.1.5",
    "jsdom": "^26.1.0",
    "openapi-typescript": "^7.10.1",
    "rimraf": "^3.0.2",
    "run-script-os": "^1.1.6",
    "tar": "^4.4.19",
    "typescript": "^5.9.2",
    "unzipper": "^0.12.3",
    "vitest": "^3.1.3"
  },
  "installConfig": {
    "hoistingLimits": "workspaces"
  },
  "resolutions": {
    "yallist": "4.0.0",
    "@types/react": "19.1.2",
    "@types/react-dom": "19.1.2"
  },
  "packageManager": "yarn@4.5.3"
}
